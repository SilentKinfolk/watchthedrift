{
  "name": "watchthedrift",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Point your camera at a Casio F-91W and see how many seconds it has drifted from real (NTP) time — right now.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
